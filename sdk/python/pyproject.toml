[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "stako"
version = "0.1.0"
description = "Thin local Python client for stako stack workflows"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["."]
include = ["stako*"]

