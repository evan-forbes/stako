description = "canonical smoke-test stack used across milestones 5-10"
created_at = 2026-05-10T14:00:00Z
paused = false
continuity = "chain"
max_concurrent_per_stack = 1
default_workdir = "/tmp/organo-smoke-workdir"
allowed_harnesses = ["claude", "codex"]
