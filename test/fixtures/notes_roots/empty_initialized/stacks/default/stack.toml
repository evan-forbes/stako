# stacks/default/stack.toml — created by `organo init`.
# All fields below are at their documented defaults. Edit freely;
# see todos/design_stack_config.md for field semantics.

description = "default stack"
created_at = 2026-05-10T14:00:00Z
paused = false
continuity = "fresh"
max_concurrent_per_stack = 1
