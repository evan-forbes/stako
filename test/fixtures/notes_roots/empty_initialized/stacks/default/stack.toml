# stacks/default/stack.toml - created by `stako init`.
# Stack config is safe to edit by hand. Stack item metadata is not.
# Use `stako stack config <name>` to inspect the daemon's parsed view.

description = "default stack"
created_at = 2026-05-10T14:00:00Z
paused = false
continuity = "fresh"
max_concurrent_per_stack = 1
